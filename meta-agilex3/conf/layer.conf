# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend \
            ${LAYERDIR}/recipes-*/*/*/*.bbappend"

BBFILE_COLLECTIONS += "altera-agilex3"
BBFILE_PATTERN_altera-agilex3 = "^${LAYERDIR}/"
BBFILE_PRIORITY_altera-agilex3 = "8"

# Depends on altera-layer (meta-altera OpenBMC integration) and the BSP
# Also depends on meta-ami (collection: meta-ami) for obmc-phosphor-image base,
# obmc-onetree-apps class, and AMI OpenBMC package groups/services.
# Depends on meta-core/meta-common (collection: common) for phosphor-* services,
# sensors, network, IPMI, certificate, host-error-monitor, and core utilities.
# altera-platform provides connectivity, benchmarking, and core bbappends from meta-altera-platform.
LAYERDEPENDS_altera-agilex3 = "altera-common phosphor-layer altera-layer altera-bsp meta-ami common altera-platform"

LAYERSERIES_COMPAT_altera-agilex3 = "walnascar"
