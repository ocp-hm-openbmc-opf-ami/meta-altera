# StartLimitIntervalSec belongs in [Unit], not [Service]. The AMI service file
# puts it in [Service] which generates a systemd warning. Clear it from [Service]
# and set it in [Unit] so the setting is applied without warnings.
[Unit]
StartLimitIntervalSec=0

[Service]
StartLimitIntervalSec=
